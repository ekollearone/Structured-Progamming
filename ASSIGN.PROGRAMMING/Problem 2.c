#include <stdio.h>
int main() {
     int amount = 10;
     float balance;
     float b = balance;
     float v;
     int m = 0;
     float i = 12;
	int mp = 10;
	float j = 0.2;
		float a1 = (j/i) ;
	   v =amount;
	   b = ((v * i) * j);
	 float b0 = (b +((j/i)* b));
	 float x = (b + b0);
	float u = ((x/i)*mp);
	while(m<=i, m++);{
	printf("balance:%.f\n", x);
	printf("AnnualInterestRate=%.1f\n", j);
	 printf("---------------\n");
	 printf("Lowest_Payment:%.f\n", u);
	  
}
	 	v = (x * mp);
	 	m = m+1;

}
